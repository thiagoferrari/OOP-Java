	// se for filha, colocar extends

    // declarar variáveis
	// codar associação (declarar var. com classe própria)

    // START GETTERS + SETTERS
	// colocar final nos sets
	// se filho, inserir somente get + set do filho
    // END GETTERS + SETTERS
    /*-----*/
    // START CONSTRUCTORS
	// se filho, chamar super nos dois contructors
	// codar agregações/composições (chamar classe)
    // END CONSTRUCTORS

    /*-----*/
    // OTHER METHODS
	// mostra()
	// se filho, trazer o super.mostra() ao final e deixar o @Override

    // fazer poimorfismo, criar um novo método na class Testa (Java Main Class)!